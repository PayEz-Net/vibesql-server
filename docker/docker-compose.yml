version: '3.8'

services:
  vibesql-server:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "52411:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - VIBESQL_CONTAINER_SECRET=${VIBESQL_CONTAINER_SECRET:-CHANGE_ME}
      - ConnectionStrings__VibeDb=Server=postgres;Port=5432;Database=vibesql;User Id=vibesql;Password=${POSTGRES_PASSWORD:-vibesql123};
      - ConnectionStrings__VibeDbRls=Server=postgres;Port=5432;Database=vibesql;User Id=vibe_rls_user;Password=${POSTGRES_PASSWORD:-vibesql123};
    depends_on:
      - postgres
    networks:
      - vibesql-network

  postgres:
    image: postgres:16-alpine
    environment:
      - POSTGRES_DB=vibesql
      - POSTGRES_USER=vibesql
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-vibesql123}
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    networks:
      - vibesql-network

volumes:
  postgres-data:

networks:
  vibesql-network:
    driver: bridge
